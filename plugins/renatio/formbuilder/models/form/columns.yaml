# ===================================
#  List Column Definitions
# ===================================

columns:

    id:
        label: renatio.formbuilder::lang.field.id

    name:
        label: renatio.formbuilder::lang.field.name
        searchable: true

    code:
        label: renatio.formbuilder::lang.field.code
        searchable: true

    template:
        label: renatio.formbuilder::lang.field.template
        relation: template
        select: code

    from_email:
        label: renatio.formbuilder::lang.field.from_email
        invisible: true

    from_name:
        label: renatio.formbuilder::lang.field.from_name
        invisible: true

    to_email:
        label: renatio.formbuilder::lang.field.to_email
        invisible: true

    to_name:
        label: renatio.formbuilder::lang.field.to_name
        invisible: true

    bcc_email:
        label: renatio.formbuilder::lang.field.bcc_email
        invisible: true

    bcc_name:
        label: renatio.formbuilder::lang.field.bcc_name
        invisible: true

    created_at:
        label: renatio.formbuilder::lang.field.created_at
        type: datetime

    updated_at:
        label: renatio.formbuilder::lang.field.updated_at
        invisible: true
        type: datetime